//checks.h
#ifndef ChecksHeader
#define ChecksHeader

/* Header file used to define function that will be used to check for common errors, so the code doesn't get cluttered with if()exits, this is possibly useless*/
#include <stdio.h>
#include <stdlib.h>

void N_check(char *str);

#endif
